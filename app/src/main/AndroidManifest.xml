<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.modulbank.accountmanager">

    <application
        android:name="ModulbankApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">

        <activity
            android:name="com.modulbank.accountmanager.activity.StartActivity">
            <intent-filter>
            <action android:name="android.intent.action.MAIN"/>
            <action android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name="com.modulbank.accountmanager.activity.signin.SignInActivity">
        </activity>

        <activity
            android:name="com.modulbank.accountmanager.activity.signup.SignUpActivity">
        </activity>

        <activity
            android:name="com.modulbank.accountmanager.activity.profile.ProfileEditorActivity">
        </activity>

        <activity
            android:name="com.modulbank.accountmanager.activity.accounts.AccountsActivity">
        </activity>

    </application>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

</manifest>
